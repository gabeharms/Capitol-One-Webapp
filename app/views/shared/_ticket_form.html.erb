<%= form_for(@ticket) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_field :title, class: "form-group", placeholder: "Whats your question..." %>
    
  </div>
  <%= f.submit "Post", class: "btn btn-primary form-group" %>
<% end %>