<%= form_for(@ticket) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div>
    <%= f.text_area :title, class: "new-ticket-field form-group", placeholder: "Whats your question..." %>  
  </div>
  <%= f.submit "Post", class: "btn btn-primary form-group" %>
<% end %>