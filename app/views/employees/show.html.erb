<!-- this page is now display_tickets.html.erb -->
<!--<% provide(:title, @employee.first_name) %>
<div class="row">
  <div class="empl-filter-bar">
  	  <div class="label">Filter:</div>
      <div class="item">Most Recent</div>
      <div class="item">Least Recent</div>
      <div class="item">Priority</div>
      <div class="item">Customer</div>
      <div class="item">Last Filter</div>
  </div>
</div>
<div class="row">
  <div>
      <h3>Tickets (<%= @tickets.count %>)</h3>
      <ol class="ticket-list">
        <% @tickets.each do |ticket| %>
          <li>
            <%= link_to(ticket.title, {:controller=>'tickets', :action => 'show', :id => ticket.id}) %>
          </li>
        <% end %>
      </ol>
      <%= will_paginate @tickets %>
  </div>
</div>-->